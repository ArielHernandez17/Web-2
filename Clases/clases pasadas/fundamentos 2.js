//operador terniario
let nombre="ariel";
let estado = false;

console.log( `
    ${estado ?'Genial ingresaste :D ' : 'No ingresaste :( '}${nombre}
    `);
console.log (`Hola ${nombre.toUpperCase()}`);

